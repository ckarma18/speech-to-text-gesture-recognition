# Speech-to-Text with Gesture Recognition

This project converts speech into text using a pretrained Whisper model and also supports a custom trained KNN + MFCC speech model. It also detects hand gestures using MediaPipe and KNN.